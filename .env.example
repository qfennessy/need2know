# Live Jev uses the configured API key.
N2K_JUDGE_MODE=jev
TYPESAFE_API_KEY=
TYPESAFE_ENDPOINT=https://api.typesafe.ai/v1/systemone
TYPESAFE_MODEL=jev-latest
N2K_DB_PATH=~/.config/need2know/need2know.db

# Give each MCP client its own registered identity in that client's config.
N2K_AGENT_ID=codex-code
